# Postgres (compatível com Supabase — connection string do painel do Supabase)
# DATABASE_URL: usada nas consultas normais do app. No Supabase, use o modo
# "Transaction pooler" (porta 6543) — melhor pra serverless (Vercel).
DATABASE_URL="postgresql://user:password@host:6543/postgres"

# DIRECT_URL: usada só pra rodar migrations. Modo "Transaction pooler" não
# suporta os locks que o Prisma Migrate precisa, então aqui use "Direct
# connection" ou "Session pooler" (porta 5432).
DIRECT_URL="postgresql://user:password@host:5432/postgres"

# URL do site (usada no servidor para montar o link/QR de autopreenchimento)
APP_URL="http://localhost:3000"

# Chave usada para assinar a sessão de login do /admin — string aleatória longa.
# Gere uma com: openssl rand -base64 32
AUTH_SECRET="troque-por-uma-string-aleatoria-de-pelo-menos-32-caracteres"

# Token de uso único pra criar o primeiro usuário via /setup?token=... sem
# precisar de acesso ao terminal/banco. Gere uma com: openssl rand -hex 16
# Depois que o primeiro usuário existir, pode remover essa variável.
SETUP_TOKEN="troque-por-um-token-aleatorio"
